# Azure Document Intelligence
# Deploy via: az deployment group create -g <rg> -f iac/main.bicep
# Get key via: az cognitiveservices account keys list --name <di-name> --resource-group <rg>
AZURE_DI_ENDPOINT=https://<your-di-resource>.cognitiveservices.azure.com
AZURE_DI_KEY=<your-document-intelligence-key>

# Model ID options: prebuilt-layout (default), prebuilt-read, prebuilt-document
# AZURE_DI_MODEL=prebuilt-layout

# ============================================
# Azure AI Foundry - GPT-4.1 (default)
# ============================================
# Get from Azure AI Foundry portal > Project > Settings > Keys and endpoint
AZURE_AI_ENDPOINT=https://<your-ai-resource>.services.ai.azure.com
AZURE_AI_API_KEY=<your-ai-foundry-key>
GPT_4_1_DEPLOYMENT=<your-gpt4-deployment-name>

# ============================================
# Azure AI Foundry - GPT-5.1 (separate endpoint)
# ============================================
# Use these if GPT-5.1 is deployed at a different endpoint
AZURE_AI_GPT5_ENDPOINT=https://<your-gpt5-ai-resource>.services.ai.azure.com
AZURE_AI_GPT5_API_KEY=<your-gpt5-ai-foundry-key>
GPT_5_1_DEPLOYMENT=<your-gpt5-deployment-name>

# ============================================
# Optional model deployments
# ============================================
# GPT_4_1_MINI_DEPLOYMENT=<optional-mini-deployment>
# TEXT_EMBEDDING_3_LARGE_DEPLOYMENT=<optional-embedding-deployment>

# Azure Blob Storage (optional - for temporary file uploads)
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...
